CHART UI FIX V2

This update fixes the screenshot issues:
- removes duplicate range controls
- chart area is light/white instead of black
- adds one Full Screen button
- fullscreen resizes the chart
- keeps date/time axis visible
- improves level-label readability
- reduces visual clutter from pattern labels
- preserves existing model/token/API logic

Replace:
templates/index.html
static/style.css

Restart:
python app.py

Then browser:
Ctrl + F5
